# scrcpy_wifi_script
scrcpy-wifi-auto is a Windows Batch Script that automates launching scrcpy. It connects an Android device via a saved Wi-Fi IP address (using ADB over TCP/IP), or directly via USB. It can save new IPs and uses optimized settings for screen mirroring.
